# 🚀 Быстрое руководство по Plop генераторам

## Доступные команды

```bash
# Запуск интерактивного меню
bun run plop

# Или вызов конкретного генератора
bun run plop component   # FSD компонент
bun run plop ui          # UI компонент в shared
bun run plop entity      # Entity с подкомпонентами
bun run plop hook        # Custom React hook
```

## ⚡ Быстрый старт

### Создать компонент в entities

```bash
bun run plop component
> Название: PostCard
> Слой: entities
> Тип: Обычный (Server Component)
> Типы: Да
> Модели: Нет
```

### Создать UI компонент

```bash
bun run plop ui
> Название: Badge
> Типы: Да
```

### Создать Entity с подкомпонентами

```bash
bun run plop entity
> Название: Post
> Подкомпоненты: PostHeader, PostContent, PostFooter
```

### Создать custom hook

```bash
bun run plop hook
> Название: useDebounce
```

## 📁 Структура FSD слоев

- **entities** - Бизнес-сущности (User, Post, Comment)
- **features** - Функциональность (LoginForm, LikeButton)
- **widgets** - Композиции (Header, Sidebar)
- **shared** - Переиспользуемое (Button, Input, hooks)

## 💡 Советы

1. **Имена компонентов** - всегда с заглавной буквы (PascalCase)
2. **Имена хуков** - всегда начинаются с "use" (useMyHook)
3. **Типы компонентов**:
   - Server Component - для статического контента
   - Client Component - для интерактивности без состояния
   - С useState - для компонентов с локальным состоянием
4. **Entity генератор** - можно запускать несколько раз для одной entity, чтобы добавить новые подкомпоненты (экспорты не дублируются)

## ⚠️ Важно

- **Entity Generator**: При добавлении новых подкомпонентов к существующей entity, главный `index.ts` автоматически обновится без дублирования экспортов
- Пример: Если в `User/index.ts` уже есть `export * from './UserAvatar'`, он не будет добавлен повторно

## 📖 Подробная документация

См. `plop-templates/README.md` для детальной информации и примеров.
