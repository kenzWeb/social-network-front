import styles from './PostCardAddComment.module.css'
import { PostCardAddCommentProps } from '../types'

export const PostCardAddComment = (props: PostCardAddCommentProps) => {
	return (
		<div className={styles.wrapper}>
			PostCardAddComment
		</div>
	)
}
